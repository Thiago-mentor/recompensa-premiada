# Opcional: se o projeto usar um Firestore além do (default), defina o mesmo ID aqui
# e configure a variável no deploy (ex.: Console Firebase → Functions → variáveis de ambiente).
# FIRESTORE_DATABASE_ID=recompensa-db
# Em produção, mantenha App Check ativo depois de validar o domínio do app.
# ENFORCE_APP_CHECK=true
